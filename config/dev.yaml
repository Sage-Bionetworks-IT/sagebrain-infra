VPC_CIDR: 10.254.174.0/24

# Optional
# CERTIFICATE_ARN: >-
#   arn:aws:acm:us-east-1:XXXXXXXXX:certificate/e8093404-7db1-4042-90d0-01eb5bde1ffc

NEPTUNE:
  instance_count: 1
  instance_class: "db.r6g.large"
  deletion_protection: false  # Allow easier cleanup in dev

NEPTUNE_SAGEMAKER:
  user_profiles:
    - thomas-yu  # SageMaker profile names: alphanumeric + hyphens only, no dots
    - avu
    - obanks

NEPTUNE_VIZ:
  enabled: true
  allowed_cidrs:
    - 52.44.61.21/32  # Sage network egress IP (VPN / office) — only source allowed to reach the UI
